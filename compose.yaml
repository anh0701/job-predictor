services:
  postgres:
    image: postgres
    container_name: job-predictor
    restart: always
    environment:
      - POSTGRES_PASSWORD=1234
    ports:
      - 5432:5432
    volumes:
      - job-predictor-vol:/usr/local/pgsql/data

volumes:
  job-predictor-vol:
    external: true